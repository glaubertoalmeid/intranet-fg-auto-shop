import {getRuntimeBucket} from "../../../../db/runtime";import {requirePermission} from "../../../../lib/auth";
export async function GET(_:Request,{params}:{params:Promise<{key:string}>}){await requirePermission("anuncios");const {key}=await params;if(!/^anuncio-[a-f0-9-]+\.(jpg|png|webp)$/.test(key))return new Response("Imagem inválida",{status:400});const o=await getRuntimeBucket().get(key);if(!o)return new Response("Imagem não encontrada",{status:404});const h=new Headers();o.writeHttpMetadata(h);h.set("etag",o.httpEtag);h.set("cache-control","private, max-age=3600");return new Response(o.body,{headers:h})}
