import {NextResponse} from "next/server";import {requirePermission} from "../../../lib/auth";import {getRuntimeDb} from "../../../db/runtime";import {initPricing} from "../pricing/route";
export async function GET(){await requirePermission("preco_ecommerce");await initPricing();const r=await getRuntimeDb().prepare("SELECT data FROM pricing_settings WHERE id=1").first<{data:string}>();return NextResponse.json(r?JSON.parse(r.data):null)}
export async function PUT(req:Request){const u=await requirePermission("preco_ecommerce");await initPricing();const data=await req.json();await getRuntimeDb().prepare(`INSERT INTO pricing_settings(id,data,updated_by,updated_at) VALUES(1,?,?,CURRENT_TIMESTAMP) ON CONFLICT(id) DO UPDATE SET data=excluded.data,updated_by=excluded.updated_by,updated_at=CURRENT_TIMESTAMP`).bind(JSON.stringify(data),u.name).run();return NextResponse.json({ok:true})}
